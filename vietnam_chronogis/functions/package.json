{
  "name": "vietnam-chronogis-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test lib/checkInValidation.test.js lib/checkInCore.test.js",
    "test:rules": "npm run build && node --test lib/firestoreRules.test.js",
    "test:rules:emulator": "firebase emulators:exec --project demo-vietnam-chronogis --config ../firebase.json --only firestore \"npm run test:rules\""
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^24.10.1",
    "firebase": "^12.15.0",
    "firebase-tools": "^15.22.4",
    "typescript": "^5.9.3"
  }
}
